const fs=require('fs'),path=require('path'),cp=require('child_process');function walk(d){return fs.readdirSync(d,{withFileTypes:true}).flatMap(x=>x.isDirectory()?walk(path.join(d,x.name)):[path.join(d,x.name)]);}const files=walk(path.join(__dirname,'..')).filter(f=>f.endsWith('.js'));let bad=0;for(const f of files){const r=cp.spawnSync(process.execPath,['--check',f],{encoding:'utf8'});if(r.status!==0){bad++;console.error(f,r.stderr);}}if(bad)process.exit(1);console.log(`Syntax OK: ${files.length} backend JS files`);
