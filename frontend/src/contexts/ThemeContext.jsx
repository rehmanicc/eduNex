import {createContext,useContext,useEffect} from 'react';import {useAuth} from './AuthContext';const T=createContext();export function ThemeProvider({children}){const{college}=useAuth();const theme=college?.theme||{};useEffect(()=>{const r=document.documentElement;r.style.setProperty('--primary',theme.primaryColor||'#1d4ed8');r.style.setProperty('--secondary',theme.secondaryColor||'#0f172a');r.style.setProperty('--accent',theme.accentColor||'#38bdf8');},[theme.primaryColor,theme.secondaryColor,theme.accentColor]);return <T.Provider value={{theme,college}}>{children}</T.Provider>}export const useTheme=()=>useContext(T);
