import * as XLSX from 'xlsx';
const n=v=>v===null||v===undefined?'—':v;function Bars({rows=[],metric='average',suffix='%'}){const max=Math.max(1,...rows.map(x=>Number(x[metric]||0)));return <div className="report-bars">{rows.slice(0,20).map(x=><div className="report-bar" key={x.id||x.label}><span title={x.label}>{x.label}</span><i><b style={{width:`${Number(x[metric]||0)/max*100}%`}}/></i><strong>{n(x[metric])}{x[metric]!==null?suffix:''}</strong></div>)}</div>}
export function ReportTable({title,rows=[]}){return <section className="analysis-block"><h3>{title}</h3><Bars rows={rows}/><div className="analysis-table-wrap"><table className="analysis-table"><thead><tr><th>Name</th><th>Students</th><th>Average %</th><th>Pass %</th><th>Highest</th><th>Lowest</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.label}</td><td>{x.students??x.responses??'—'}</td><td>{n(x.average??x.averageRating)}</td><td>{n(x.passRate)}</td><td>{n(x.highest)}</td><td>{n(x.lowest)}</td></tr>)}{!rows.length&&<tr><td colSpan="6">No data for these filters.</td></tr>}</tbody></table></div></section>}
export function FeedbackBreakdown({title,rows=[]}){return <section className="analysis-block"><h3>{title}</h3><Bars rows={rows} metric="averageRating" suffix=" / 5"/><div className="analysis-table-wrap"><table className="analysis-table"><thead><tr><th>Name</th><th>Responses</th><th>Average Rating</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.label}</td><td>{x.responses}</td><td>{n(x.averageRating)}{x.averageRating!==null?' / 5':''}</td></tr>)}{!rows.length&&<tr><td colSpan="3">No data for this breakdown.</td></tr>}</tbody></table></div></section>}
export function exportWorkbook(name,sheets){const wb=XLSX.utils.book_new();Object.entries(sheets).forEach(([k,v])=>XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(v||[]),k.slice(0,31)));XLSX.writeFile(wb,`${name}.xlsx`)}
export function AnalyticsReportHeader({college,title,subtitle}){return <header className="analytics-report-header"><div className="analytics-report-logo">{college?.logoUrl?<img src={college.logoUrl} alt="College logo"/>:<span>LOGO</span>}</div><div className="analytics-report-identity"><h1>{college?.name||'College'}</h1><div className="analytics-report-slogan">{college?.educationalSlogan||''}</div><h2>{title}</h2>{subtitle&&<p>{subtitle}</p>}</div></header>}
export function PrintButtons({onExcel}){const print=()=>{const report=document.querySelector('.analytics-print-report');if(!report)return;const win=window.open('','_blank','width=1000,height=900');if(!win)return;const styles=[...document.querySelectorAll('link[rel="stylesheet"],style')].map(node=>node.outerHTML).join('');win.document.open();win.document.write(`<!doctype html><html><head><meta charset="utf-8"><title>Analytics Report</title>${styles}<style>@page{size:A4 portrait;margin:8mm}html,body{margin:0!important;padding:0!important;background:#fff!important}body{display:block!important}.analytics-print-report{position:static!important;display:block!important;visibility:visible!important;width:100%!important;margin:0!important;padding:0!important;box-sizing:border-box!important}.analytics-print-report,.analytics-print-report *{visibility:visible!important}.analytics-report-header{display:grid!important;margin-top:0!important}.analysis-actions{display:none!important}.analysis-block{break-inside:avoid;box-shadow:none!important}.report-bars b{-webkit-print-color-adjust:exact!important;print-color-adjust:exact!important}</style></head><body>${report.outerHTML}</body></html>`);win.document.close();const doPrint=()=>{win.focus();win.print();};if(win.document.readyState==='complete')setTimeout(doPrint,250);else win.addEventListener('load',()=>setTimeout(doPrint,250),{once:true});};return <div className="analysis-actions"><button type="button" onClick={print}>Print / PDF</button><button type="button" onClick={onExcel}>Export Excel</button></div>}
